{% extends  "index.html.twig" %}
{%  block menu %}   
    {% include 'Parts/menu.html.twig' %}
{% endblock %}

{% block header %}
   
{% endblock %}


{% block body %}
    <div class="row d-flex justify-content-center">
        <div class="col-8 border border-1 p-3">
            {{ form_start(form) }}
                <div class="row">
                    <div class="col-6">
                        {{ form_row(form.PartBrand.name, {'attr': {'class': 'form-control-sm'}}) }}
                    </div>
                    <div class="col-6">
                        {{ form_row(form.numberText, {'attr': {'class': 'form-control-sm'}}) }}
                    </div>
                </div>
            
                <div class="row">
                    <div class="col-12">
                        {{ form_row(form.info, {'attr': {'class': 'form-control-sm'}}) }}
                    </div>
                </div>
                  
                    <button type="submit" class="btn-sm btn-outline-primary btn">Сохранить</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}



{% block javascripts %}
  {{ parent() }}

{% endblock %}